<div class="col-sm-6 col-md-4 my-4">
  <div class="card">
    <div class="card-body">
      <h5 class="card-title"><%= video.name %></h5>
      <p class="card-text"><%= video.description %></p>
      <%= link_to 'View', video_path(video), class: 'btn btn-primary' %>
    </div>
  </div>
</div>